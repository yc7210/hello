/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
export interface ErrorParams {
    message: string;
    code: number;
    name: string;
    details?: string;
    params?: {
        [key: string]: unknown;
    };
    cause?: Error;
}
/**
 * Class describing errors encountered when using the AmazonIVSBroadcastClient.
 */
export declare class BroadcastClientError extends Error {
    code: number;
    name: string;
    details?: string;
    params?: {
        [key: string]: unknown;
    };
    cause?: Error;
    /**
     * Creates the new error.
     *
     * @param parameters - Object containing the Error parameters.
     * @param parameters.name - The name of the error.
     * @param parameters.message - The message of the error.
     * @param parameters.code - The code for the error.
     * @param parameters.details - Additional details and long-form message for the error.
     * @param parameters.cause - Stack trace for the error.
     * @param parameters.params - Object containing additional parameters to attach to the error.
     */
    constructor({ name, message, code, details, cause, params }: ErrorParams);
}
