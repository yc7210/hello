/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
export declare type Report = RTCTransportStats & {
    [key: string]: unknown;
};
import { ConnectionState } from './broadcast-client-events';
/**
 * TODO: PeerClient interface that must be implemented by either client
 */
export interface BasePeerClient {
    start(): Promise<void>;
    stop(): void;
    getConnectionState(): ConnectionState;
    addTracks(data: {
        tracks: MediaStreamTrack[];
    }): void;
    getReports(): Report[];
}
