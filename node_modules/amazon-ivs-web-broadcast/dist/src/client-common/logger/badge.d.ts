/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
import { LogLevels, LoggerScope } from './logger.types';
export interface Badge {
    label: keyof typeof logBackgroundColor;
}
declare const logBackgroundColor: {
    2: string;
    3: string;
    1: string;
    4: string;
    0: string;
    5: string;
    SPADE: string;
    Time: string;
    WebRTC: string;
    StageSubscription: string;
    StageConnection: string;
    StageSocket: string;
    Emitter: string;
    LocalParticipant: string;
    Stage: string;
    RemoteParticipant: string;
    details: string;
};
/**
 * Creates an array off CSS strings that create 'badges' in the logger.
 *
 * @param level - The log level to include as a badge.
 * @param scope - The scope of the log to include as a badge.
 * @param details - Additional details to include as a badge.
 * @private
 */
export declare const getBadges: (level: ValuesOf<typeof LogLevels>, scope?: LoggerScope, details?: string) => string[];
export {};
