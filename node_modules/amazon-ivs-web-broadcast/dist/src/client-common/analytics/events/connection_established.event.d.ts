import { type AnalyticsEvent } from './base-analytics.types';
export declare class ConnectionEstablishedEvent implements AnalyticsEvent {
    data: AnalyticsEvent['data'];
    constructor(connectionEstablishedDate: Date, connectionStartDate: Date | undefined, ingestSessionId: string | undefined);
    transformProperties: (connectionEstablishedDate: Date, connectionStartDate: Date | undefined, ingestSessionId: string | undefined) => {
        duration: number;
        ecn_negotiated: boolean;
        ingest_session_id: string | undefined;
    };
}
