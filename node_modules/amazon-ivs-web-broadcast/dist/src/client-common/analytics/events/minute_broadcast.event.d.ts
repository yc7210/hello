/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
import { type Report } from '../../peer-client.types';
import { type AnalyticsEvent } from './base-analytics.types';
export declare class MinuteBroadcastEvent implements AnalyticsEvent {
    data: AnalyticsEvent['data'];
    constructor(report: Report, startTimestamp: number);
    transformProperties: (report: Report, startTimestamp: number) => {
        minutes_logged: number;
    };
}
