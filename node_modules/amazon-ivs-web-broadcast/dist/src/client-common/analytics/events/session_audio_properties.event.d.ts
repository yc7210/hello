import { type AnalyticsEvent } from './base-analytics.types';
export declare class SessionAudioPropertiesEvent implements AnalyticsEvent {
    data: AnalyticsEvent['data'];
    constructor(audioSettings: MediaTrackSettings);
    transformProperties: (audioSettings: MediaTrackSettings) => {
        codec: string;
        bitrate: number;
        sample_rate: number | undefined;
        channel_count: number;
    };
}
