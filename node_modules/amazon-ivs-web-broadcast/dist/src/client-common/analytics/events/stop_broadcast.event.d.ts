import { type AnalyticsEvent, type BroadcastEndpoint } from './base-analytics.types';
export declare class StopBroadcastEvent implements AnalyticsEvent {
    data: AnalyticsEvent['data'];
    constructor(properties: BroadcastEndpoint);
    transformProperties: ({ protocol, hostname, port }: BroadcastEndpoint) => {
        protocol: string;
        endpoint_host: string;
        endpoint_port: string;
    };
}
