/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
import { SDPFmtpLineParams } from '../../sdp-ftmp-line';
import { type AnalyticsEvent } from './base-analytics.types';
export declare class SessionVideoEncoderConfiguredEvent implements AnalyticsEvent {
    data: AnalyticsEvent['data'];
    constructor(videoSettings: MediaTrackSettings, sdpFmtpLineParams: SDPFmtpLineParams, codec: RTCRtpCodecParameters);
    transformProperties: (videoSettings: MediaTrackSettings, sdpFmtpLineParams: SDPFmtpLineParams, codec: RTCRtpCodecParameters) => {
        codec: string;
        codec_profile: string;
        codec_level: number;
        rate_mode: string;
        initial_bitrate: number;
        keyframe_interval: number;
        width: number | undefined;
        height: number | undefined;
        bframe_count: number;
        target_fps: number | undefined;
    };
}
