import { type Samples } from '../samples';
import { type AnalyticsEvent } from './base-analytics.types';
export declare class SourceAudioLatencyEvent implements AnalyticsEvent {
    data: AnalyticsEvent['data'];
    constructor(samples: Samples, period: number);
    transformProperties: (samples: Samples, period: number) => {
        sum: number;
        median: number;
        min: number;
        max: number;
        p90: number;
        event_count: number;
        period: number;
    };
}
