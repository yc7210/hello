/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
import { StartBroadcastReason } from '../events';
import { type AnalyticsEvent, type BroadcastEndpoint } from './base-analytics.types';
export declare class StartBroadcastEvent implements AnalyticsEvent {
    data: AnalyticsEvent['data'];
    constructor(properties: BroadcastEndpoint, reason: StartBroadcastReason);
    transformProperties: ({ protocol, hostname, port }: BroadcastEndpoint, reason: StartBroadcastReason) => {
        protocol: string;
        endpoint_host: string;
        endpoint_port: string;
        reason: StartBroadcastReason;
    };
}
