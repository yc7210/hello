import { type AnalyticsEvent, type ErrorEvent as IErrorEvent } from './base-analytics.types';
export declare class ErrorEvent implements AnalyticsEvent {
    data: AnalyticsEvent['data'];
    constructor(error: Partial<IErrorEvent>);
    transformProperties: (error: Partial<IErrorEvent>) => {
        is_fatal: boolean;
        value: number | undefined;
        code: number | undefined;
        description: string | undefined;
    };
}
