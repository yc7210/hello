import { type AnalyticsEvent } from './base-analytics.types';
export declare class SessionVideoPropertiesEvent implements AnalyticsEvent {
    data: AnalyticsEvent['data'];
    constructor(videoSettings: MediaTrackSettings);
    transformProperties: (videoSettings: MediaTrackSettings) => {
        codec: string;
        initial_bitrate: number;
        min_bitrate: number;
        max_bitrate: number;
        keyframe_interval: number;
        width: number | undefined;
        height: number | undefined;
        auto_bitrate_enabled: boolean;
        bframes_enabled: boolean;
        target_fps: number | undefined;
    };
}
