import { type AnalyticsEvent } from './base-analytics.types';
export declare class SessionAudioEncoderConfiguredEvent implements AnalyticsEvent {
    data: AnalyticsEvent['data'];
    constructor(codec: RTCRtpCodecParameters);
    transformProperties: (codec: RTCRtpCodecParameters) => {
        codec: string;
        bitrate: number;
        sample_rate: number;
        channel_count: number | undefined;
    };
}
