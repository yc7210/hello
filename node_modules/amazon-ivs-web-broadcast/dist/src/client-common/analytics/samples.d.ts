/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
/**
 * Stateful class containing calculated sample data.
 */
export declare class Samples {
    min: number;
    max: number;
    sum: number;
    data: number[];
    /**
     * Creates an instance of [[Samples]].
     */
    constructor();
    /**
     * Calculates the new minimum, maximum, and sum based on the provided value.
     *
     * @param n - A number representing a single sample.
     */
    push(n: number): void;
    /**
     * Calculates the median value from the current recorded samples.
     */
    median(): number;
    /**
     * Calculates a pXX value based on the recorded samples.
     *
     * @param p - A percentile value ranging from [0,1]
     * @example
     * ```typescript
     * const [1,2,3,4,5];
     * const samples = new Samples();
     * for (const value of arrayOfValues) {
     *   samples.push(value);
     * }
     * // get the p90
     * const percentile = samples.percentile(0.9);
     * // percentile === 4.6;
     * ```
     */
    percentile(p: number): number;
}
