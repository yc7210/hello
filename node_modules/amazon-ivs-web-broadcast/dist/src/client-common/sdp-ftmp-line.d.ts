/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
export interface SDPFmtpLineParams {
    levelAsymmetryAllowed: boolean;
    packetisationMode: boolean;
    profile: string;
    level: number;
}
/**
 * Utility class providing the ability to parse the SDPFmtpLine.
 * The SDPFmtpLine is a DOMString containing the format-specific parameters field from the "a=fmtp" line in the codec's SDP, if one is present;
 * [see section 5.8 of the IETF specification for JSEP](https://datatracker.ietf.org/doc/html/draft-ietf-rtcweb-jsep-24#section-5.8)
 */
export declare class SDPFmtpLine {
    levelAsymmetryAllowed: SDPFmtpLineParams['levelAsymmetryAllowed'];
    packetisationMode: SDPFmtpLineParams['packetisationMode'];
    profile: SDPFmtpLineParams['profile'];
    level: SDPFmtpLineParams['level'];
    /**
     * Extracts the values from the SDP Fmtp Line.
     *
     * @param line - An SDP Fmtp Line.
     * @returns {SDPFmtpLineParams} Parsed SDP Fmtp Line data; profile, level, packetization mode, level asymmetry allowed
     */
    extractVideoSdpFmtpLine(line?: string): SDPFmtpLineParams;
    /**
     * Parses an id from the profile-level-id field and returns its profile.
     *
     * @param profileLevelId - A hex value representing a profile.
     * @returns {string} the profile as string.
     */
    private getProfileFromId;
    /**
     * Parses an id from the profile-level-id field and returns its level.
     *
     * @param profileLevelId - A hex value representing a profile.
     * @returns {number} the level as a number.
     */
    private getLevelFromId;
}
