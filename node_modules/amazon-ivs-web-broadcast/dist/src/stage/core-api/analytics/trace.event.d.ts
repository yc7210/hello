/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
import { Action } from '../constants';
import { StageToken } from '../token';
import { StageAnalyticsEvent } from './event';
import { TraceId } from './random.uuid';
/**
 * An event class for _trace() API calls.
 */
declare class TraceEvent extends StageAnalyticsEvent {
    /**
     * Creates a TraceEvent.
     *
     * @param token - Caller participant's stage token.
     * @param traceId - Trace identifier used to group actions together.
     * @param message - Core event message.
     * @param action - Action performed.
     * @param remoteParticipantID - Optional remote participant identifier.
     */
    constructor(token: StageToken, traceId: TraceId, message: string, action: Action, remoteParticipantID?: string);
}
export { TraceEvent };
