import { StageToken } from '../token';
import { StageAnalyticsEvent } from './event';
import { TraceId } from './random.uuid';
/**
 * A class derived from StageAnalyticsEvent to handle Minutes published stats.
 */
export declare class MinuteEvent extends StageAnalyticsEvent {
    /**
     * Creates a MinuteEvent.
     *
     * @param token - Caller participant's stage token.
     * @param traceId - Trace identifier used to group actions together.
     * @param minutesPublished - Number of minutes of content published.
     * @param isPublishing - Is the participant currently publishing content.
     * @param subscribedNumber - Number of remote participants that this local participant is subscribed to.
     */
    constructor(token: StageToken, traceId: TraceId, minutesPublished: number, isPublishing: boolean, subscribedNumber: number);
}
