import { type StageToken } from '../token';
import { StageAnalyticsEvent } from './event';
import { TraceId } from './random.uuid';
export declare const LEAVE_REASON = "websdk";
export declare class LeaveEvent extends StageAnalyticsEvent {
    constructor(properties: {
        token: StageToken;
        eventEndpoint: string;
        whipEndpoint: string;
        traceId: TraceId;
        reason?: string;
    });
}
