import { TraceId } from './analytics/random.uuid';
import { Action } from './constants';
declare function getTurnCredentials({ action, identifier, token, traceId, tag, }: {
    action: Action;
    identifier: string;
    token: string;
    traceId: TraceId;
    tag: string;
}): Promise<{
    iceCredentials: RTCIceServer[];
    responseURL: any;
} | undefined>;
export { getTurnCredentials };
